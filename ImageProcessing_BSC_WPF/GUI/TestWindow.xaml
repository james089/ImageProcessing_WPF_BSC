<Window x:Class="ImageProcessing_BSC_WPF.TestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ImageProcessing_BSC_WPF"
        mc:Ignorable="d"
        Title="TestWindow" Height="300" Width="300" Background="White" Loaded="Window_Loaded">
    <Window.Resources>
        <ResourceDictionary Source="/mUserControl_BSC_dll;component/Themes/CloudPoint.xaml"/>
    </Window.Resources>
    <StackPanel>
        <ToggleButton Content="Sth"/>
        <Expander Header="344" Width="200" Background="White" >
            <GroupBox Header="GroupBox" FontSize="20" >
                <Button Content="22" />
            </GroupBox>
        </Expander>

        <Grid Width="100">
            <ScrollViewer x:Name="SV" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled">
                <TextBlock x:Name="TBL_longtext" Text="This is a super long text that is showing in a loop"/>
            </ScrollViewer>
        </Grid>
    </StackPanel>
</Window>
